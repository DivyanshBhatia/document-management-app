import React from 'react';
import ReactDOM from 'react-dom/client';
import DocumentManagementApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DocumentManagementApp />
  </React.StrictMode>
);
